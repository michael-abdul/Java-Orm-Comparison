logging:
  level:
    root: warn
    org.jooq.tools.LoggerListener: warn
    win.doyto.query.sql.SqlAndArgs: warn

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    # HeidiSQL dagi ma'lumotlar asosida:
    url: jdbc:postgresql://127.0.0.1:5432/postgres 
    username: postgres
    password: povice1004
